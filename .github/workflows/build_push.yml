name: CI

on:
  push:
    branches:
      - main
    paths:
      - '**'
      - '!**.md'
      - '!.github/**'
  workflow_dispatch:

permissions: {}

concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: true

jobs:
  build:
    name: Build ProChan Extension
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Checkout main branch
        uses: actions/checkout@v4

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          java-version: 17
          distribution: temurin

      # تفعيل الكاش لتسريع عملية البناء في المرات القادمة بشكل هائل
      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          cache-encryption-key: ${{ secrets.GRADLE_ENCRYPTION_KEY }} # اختياري

      - name: Run Spotless Apply
        run: |
          ./gradlew spotlessApply

      # بناء الإضافة مباشرة بدون خطوات التحضير المعقدة
      - name: Build ProChan
        run: |
          ./gradlew :src:ar:procomic:assembleDebug

      # رفع ملف الـ APK الناتج كـ Artifact
      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: "ProChan-APK"
          path: "**/*.apk"
          retention-days: 5
